# 閃動格子 — 遊戲部 專案管理工具

## 專案概述
遊戲部內部專案管理工具，用於團隊成員回報與管理專案最新狀態。部署於 GitHub Pages，資料儲存於 Firebase Firestore（game-pm-system02）。

## 技術架構
- **前端**：Vanilla JS + HTML + CSS（無框架）
- **資料庫**：Firebase Firestore（compat SDK，瀏覽器 script 載入）
- **部署**：GitHub Pages
- **外部整合**：Discord Webhook（標註推送通知）

## 檔案結構
- `index.html` — 主頁面 HTML
- `app.js` — 所有邏輯（資料、UI、Firestore 同步、通知、拖曳等）
- `styles.css` — 所有樣式
- `migrate.html` — 從舊版系統遷移資料的工具

## 資料結構
- **TRUNKS**：主幹陣列，每個主幹內嵌 `branches` 枝幹陣列
- **NODES**：留言泡泡（節點），屬於特定 trunk + branch
- **MEMBERS**：成員（有 active 欄位控制在職/停用）
- **DAILY_REPORTS**：每日進度
- **MENTIONS**：@標註通知記錄
- **CATS**：每日進度工作類別
- **REPORT_TYPES**：回報類型（進度回報、里程碑等）
- **PROJECT_STATUSES**：專案狀態（待處理、製作中、測試中、已完成、擱置中）
- **PRIORITIES**：優先度等級

## 功能清單

### 時間軸系統（核心）
- 主幹/枝幹樹狀結構，主幹白底黑邊，枝幹依專案狀態顯色
- 高優先度主幹以紅色橫條+紅色名稱+左側紅色光暈顯示
- 展開的高優先度枝幹也有紅色光暈
- 背景區分平日（白）、週末（灰底）、國定假日（紅底）
- 橫條可拖曳調整起迄日期，或拖曳整條平移
- 時間粗度為 110px/天（2.5 倍），可縮放 50%~200%
- 「今天」按鈕快速定位

### 留言泡泡
- 預設唯讀模式顯示完整內容、圖片、連結
- 右上角「編輯」按鈕切換編輯模式
- Google 風格回覆串，可在留言底下回覆
- 收合時顯示 💬 回覆數量
- 支援 @成員名稱 標註（中英文皆可）
- 同位置多則留言自動堆疊顯示 badge，點擊展開選擇器
- 支援圖片上傳、連結附件

### @標註與通知
- Discord Webhook 自動推送（FormData 格式避免 CORS）
- Webhook URL：見 app.js 中 DISCORD_WEBHOOK 常數
- 右上角 🔔 通知鈴鐺，顯示今日未讀標註數
- 通知面板列出今日標註，已讀變暗淡
- 點擊通知跳到對應留言（URL ?node=xxx 參數）

### 專案總表
- 表格檢視所有主幹/枝幹狀態
- 主幹有可編輯的「專案狀況」欄位（寶藍色）
- 枝幹顯示最新留言
- 匯出 Excel / 列印 PDF
- 「📦 已封存」按鈕切換查看/還原封存項目

### 篩選系統
- 右上角負責人篩選下拉選單（同時比對主幹和枝幹負責人）
- 篩選啟用時下拉選單加藍色光暈脈動動畫提示
- Header 成員頭像點擊篩選特定成員的留言

### 每日進度
- 底部面板，選擇日期+成員填寫每日工作
- 每項可選枝幹歸屬和工作類別
- 勾選完成自動生成時間軸留言泡泡
- 已封存項目不會出現在選擇器中

### 週報總覽
- 表格呈現一週內每位成員每天的進度
- 可前後翻頁切換週次

### 封存功能
- 右鍵選單「📦 封存」
- 主幹封存 → 整批枝幹一起封存
- 枝幹可獨立封存
- 專案總表可查看/還原封存項目

### 成員管理
- 新增、改名、改色、刪除成員
- 「在職/已停用」切換（停用後不出現在選擇器，歷史資料保留）

### 設定頁面（5 個分頁）
- 👥 成員（含在職/停用切換）
- 🏷️ 回報類別（進度回報、里程碑等的顏色標籤）
- 📌 專案狀態（待處理、製作中等的定義與顏色）
- 🔥 緊急程度（優先度等級）
- 📂 進度類別（每日進度的工作分類，可自由新增刪改）

### 其他功能
- Ctrl+Z / Cmd+Z 復原（最多 30 步）
- 左側欄位可拖曳排序主幹
- 枝幹可拖曳獨立或併入其他主幹
- 左側欄位寬度可拖曳調整
- 右側 detail panel（主幹：優先度、負責人、追蹤者、說明、連結、統計；枝幹：期間、負責人、說明、連結、統計）
- 右鍵選單（重新命名、變更顏色、封存、刪除）
- 枝幹也可指派負責人
- 主幹狀態被動統計自枝幹（無自身狀態）
- Firestore 即時同步，支援多人同時操作
- 遷移工具（migrate.html）從舊版系統匯入

## 設計原則
- 主幹永遠灰白色調（白底黑邊 bar、黑色名稱文字），不帶自身顏色
- 枝幹顏色 = 專案狀態色
- 高優先度 = 紅色視覺（bar、名稱、光暈）
- 已完成/擱置中 = 50% 透明度 + 低飽和
- 停用成員歷史資料保留顯示
- 封存項目隱藏但不刪除
